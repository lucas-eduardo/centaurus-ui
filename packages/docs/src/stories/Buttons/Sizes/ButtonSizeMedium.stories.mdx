import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';

import { Button, ButtonProps } from 'ds-react'

<Meta
  title="Components/Button/Size/Medium"
  component={Button}
  parameters={{
    viewMode: 'docs'
  }}
/>

export const Template = args => <Button {...args} />

# Botão size Small

<Canvas>
  <Story
    name="Medium Text"
    args={{
      children: 'Medium Text',
      size: 'medium'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Medium Contained"
    args={{
      children: 'Medium Contained',
      variant: 'contained',
      size: 'medium'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Medium Outlined"
    args={{
      children: 'Medium Outlined',
      variant: 'outlined',
      size: 'medium'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
