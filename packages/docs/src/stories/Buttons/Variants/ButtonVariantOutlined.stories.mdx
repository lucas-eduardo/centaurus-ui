import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';

import { Button, ButtonProps } from 'ds-react'

<Meta
  title="Components/Button/Variants/Outlined"
  component={Button}
  parameters={{
    viewMode: 'docs'
  }}
/>

export const Template = args => <Button {...args} />

# Botão variante Outlined

<Canvas>
  <Story
    name="Outlined Primary"
    args={{
      children: 'Primary',
      variant: 'outlined'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Outlined Disabled"
    args={{
      children: 'Disabled',
      variant: 'outlined',
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Outlined Link"
    args={{
      children: 'Link',
      as: 'link',
      variant: 'outlined'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
